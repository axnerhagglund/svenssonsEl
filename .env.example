# Optional: canonical public URL for your production domain.
# Vercel preview/production still work without this (they use VERCEL_URL).
# Set this in Vercel → Project → Settings → Environment Variables (Production)
# Example: https://svenssonel.se
NEXT_PUBLIC_SITE_URL=
